{
  "name": "@zawa/rdm-ingest",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.dev.vars --import tsx src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"I don't need tests, I don't want tests. Do not add them.\" && exit 0"
  },
  "dependencies": {
    "@zawa/domain": "workspace:*",
    "@zawa/rdm": "workspace:*",
    "@zawa/shared": "workspace:*",
    "kafkajs": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
